Não esquecer de:

Acertar a posição das variáveis de entrada/saída
Acertar a variável com o comando: $ export GOOGLE_APPLICATION_CREDENTIALS='/home/gpca/Downloads/key.json' (Linux - Ubuntu)
NÃO TESTADO EM WINDOWS, usar apenas Linux.
Caso necessário, realizar a autenticação do Google para utilizar o algoritmo. Para isso, terá de fazer a autenticação pelo $ gcloud auth login
Não esquecer de acertar o projeto! Utilize $ gcloud config set project <id do projeto>

Divirta-se!

